spring:
  application:
    name: tagtrace
  datasource:
    url: jdbc:h2:mem:tagtrace
    username: sa
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true